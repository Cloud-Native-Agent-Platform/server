apiVersion: v1
kind: Service
metadata:
  name: cnap-server
  namespace: cnap-dev
  labels:
    app.kubernetes.io/name: cnap-server
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: cnap-server
